# 官方施工方法示例模型 —— 待内置（占位）

规划：把 MIDAS 官方教程中几座**施工方法**代表桥解析后内置到 `assets/示例/<工法>/`，作为各 `reference/施工方法/<工法>.md` 的参照模型——**只借结构与工法节拍，取值仍按铁律回规范 / 设计图**。**当前暂不入库，先占位。**

## 计划清单
> 源在开发期目录 `midas_official_examples/`（**不随 skill 发布**，仅实施时取用）。

| 工法 | 子目录（拟） | 源模型 | 现状 |
|---|---|---|---|
| 悬臂浇筑 FCM | `FCM-悬臂浇筑/` | `11 FCM Wizard.mcb` + `.mct` | 有 .mcb+.mct，可直接精简内置 |
| 顶推 ILM | `顶推-ILM/` | —（官方仅 HTML 教程，无模型文件）| 只能内置工法文档，无模型 |
| 斜拉 PC | `斜拉-PC/` | `12 PC Cable-stayed_Part I/II.mcb` | 有 .mcb、无 .mct |
| 拱 | `拱-AASHTO/` | `Tut 4 Arch Bridge - AASHTO LRFD.mcb` | 有 .mcb、无 .mct |
| 钢混组合梁 | `组合梁-钢I/` | `7 Steel I-Girder Composite Bridge.mcb` | 有 .mcb、无 .mct |

## 入库规则（实施时遵守）
1. **只留精简件**：`.mcb`（+ 有则 `.mct`）。**排除**巨型副产物——`.CA6`（可达 ~200 MB）/ `.OUT` / `.bak` / `.spa` / `.zip` / `.xls` / 教程 PDF。
2. **缺 `.mct` 的**（拱 / 组合 / 斜拉）：用 `midas_official_examples/export_mct.py` 跑一次 MIDAS 往返导出文本源——**会跑 MIDAS，单列为后置步骤**，确认后再做。
3. 内置完，对应 `reference/施工方法/<工法>.md` 引用本目录模型。
4. 来源为 MIDAS 自带教程（第三方版权），仅作内部参照。
